# Web framework
# fastapi 0.138.x drops Starlette's upper bound, letting us pull a Starlette
# release with no open advisories (CVE remediation — see requirements.lock).
fastapi==0.141.1
uvicorn[standard]==0.52.3

# Configuration
pydantic-settings==2.15.0

# Database - PostgreSQL
sqlalchemy==2.0.52
alembic==1.19.1
asyncpg>=0.31.0
psycopg2-binary>=2.9.12

# Oracle connectivity (optional — install separately; see requirements-oracle.txt)
# python-oracledb is imported lazily inside ConnectionService.test_connection()
# so the app runs without it; only Oracle connectivity tests will fail at runtime.

# Scheduler
apscheduler==3.11.3

# Auth
PyJWT==2.13.0
bcrypt==5.0.0

# Encryption (credential storage at rest)
cryptography==50.0.0

# Logging
structlog==26.1.0

# HTTP client (for health checks, internal calls)
httpx==0.28.1

# Development / testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# Linting and type checking
ruff==0.16.3
mypy==2.3.0
